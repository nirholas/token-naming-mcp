{
	"$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
	"name": "io.github.nirholas/naming-mcp",
	"title": "three.ws Naming",
	"description": "On-chain identity for AI agents: resolve .sol names, reverse-lookup wallets, check handles.",
	"websiteUrl": "https://three.ws",
	"icons": [
		{
			"src": "https://three.ws/three-ws-mcp-icon.svg",
			"mimeType": "image/svg+xml",
			"sizes": [
				"any"
			]
		}
	],
	"repository": {
		"url": "https://github.com/nirholas/naming-mcp",
		"source": "github"
	},
	"version": "0.1.0",
	"packages": [
		{
			"registryType": "npm",
			"identifier": "@three-ws/naming-mcp",
			"version": "0.1.0",
			"runtimeHint": "npx",
			"transport": {
				"type": "stdio"
			},
			"environmentVariables": [
				{
					"name": "THREE_WS_BASE",
					"description": "Base URL of the three.ws API that serves /api/sns and /api/threews/subdomain. Override only when self-hosting or targeting a preview deployment.",
					"format": "string",
					"isRequired": false,
					"default": "https://three.ws"
				},
				{
					"name": "THREE_WS_TIMEOUT_MS",
					"description": "Per-request timeout in milliseconds. Resolution and availability checks hit a Solana RPC pool, so the default leaves room for a cold lookup.",
					"format": "number",
					"isRequired": false,
					"default": "20000"
				}
			]
		}
	]
}
